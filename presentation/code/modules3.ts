// single import
const myDefaultFunction = require('./myModule');
// mulitple imports (using destructuring syntax)
const { myFunction, myConstant } = require('./myModule');
// importing from external library
const animals = require('animals');
const bear = require('animals').bear;