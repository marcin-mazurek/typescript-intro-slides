let name = 'John';
let surname = 'Doe';
let fullName = name + ' ' + surname;
let isAdmin = false;

let user = {
  name: name,
  surname: surname,
  isAdmin: isAdmin,
};

let a = 1;
let arr = [1, 2, 3, 4, 5];

function add(a, b) {
  return a + b;
}
add(1, 2);

// a single line comment
/* a multi-line
   comment */